# Develop override settings

url: http://localhost:4000

analytics:
  provider: false

comments:
  disqus:
    shortname            : "mmistakes-dev"

sass:
  sass_dir: _sass
  load_paths:
    - _sass
  style: expanded
